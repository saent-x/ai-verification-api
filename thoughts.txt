ROUTE: /verification?mode=identity&api_key=<user_key_here>

DESCRIPTION: Checks that the selfie provided is the same face on the forms of id provided 

PAYLOAD:
{
    prefix: <six-digit-prefix>
    storageMode: ["azure" | "s3" | "google-cloud" | "headless" (default)] 
    # if storageMode === "azure"
        connectionString: <connection-string>
    #end if
    # if storageMode === "s3"
        accessKeyId: <user-access-key-api>
        secretAccessKey: <user-secret-access-key>
    #end if
    # if storageMode === headless
        selfie: <base-64-encoding-of-selfie>
        id1: <base-64-encoding-of-id>
        id2: <base-64-encoding-of-id>
        ...
}

RESPONSE: 
{
    score: <percentage of correlation>
}

ROUTE: /verification?mode=document&api_key=<user_key_here>